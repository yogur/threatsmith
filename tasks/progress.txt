## Codebase Patterns
- New skills-src/ tree: authored skill sources live in `skills-src/` at repo root; shared reference markdown is in `skills-src/_shared/references/`. Never edit files under `build/skills/` directly — those are generated by the build step.
- Reference content source of truth: `skills-src/_shared/references/` holds the canonical markdown files. The Python constants in `src/threatsmith/frameworks/references/` remain for now (they back the current assembler) but will be retired as US-007/US-008/US-009 land.

---

## 2026-06-16 - US-001
- Created `skills-src/_shared/references/` directory with six canonical shared reference markdown files:
  - `stride-categories.md` — STRIDE threat category table (from `stride_categories.py`)
  - `owasp-web-top-10.md` — OWASP Top 10 2025 (from `owasp.py` `OWASP_WEB_TOP_10`)
  - `owasp-api-top-10.md` — OWASP API Security Top 10 2023 (from `owasp.py` `OWASP_API_TOP_10`)
  - `owasp-llm-top-10.md` — OWASP LLM Top 10 2025 (from `owasp.py` `OWASP_LLM_TOP_10`)
  - `owasp-mobile-top-10.md` — OWASP Mobile Top 10 2023 (from `owasp.py` `OWASP_MOBILE_TOP_10`)
  - `scanners.md` — How to run Semgrep, Trivy, Gitleaks; agent detects availability and runs what's present (consolidated from per-scanner snippets in `scanner_snippets.py`)
- Files changed: `skills-src/_shared/references/` (6 new files), `tasks/stories.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - The existing Python constants (`owasp.py`, `stride_categories.py`, `scanner_snippets.py`) are NOT deleted here — they still back the current assembler. They get removed in US-007/US-008/US-009.
  - `scanners.md` is authored as a detection-first guide (agent checks availability then runs what exists) rather than assuming a specific scanner is present — this matches the PRD requirement that the agent handles scanner detection entirely.
  - No new Python code was needed; this story is pure file authoring. Tests pass because existing test coverage doesn't touch the new `skills-src/` tree.
---
